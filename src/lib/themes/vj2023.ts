import type { LayoutTheme } from "$lib/models/LayoutConf";

export const vj2023: string = `
    --font-family: sans-serif;
    --font-color: white;
    --donation-bar-padding: 0 10px;
    --donation-bar-background: black;
    --donation-bar-fill: green;
`;
